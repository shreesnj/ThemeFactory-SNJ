{% liquid
  assign section_id = section.id
%}

<footer
  id="section-{{ section_id }}"
  class="snj-footer color-{{ section.settings.color_scheme }}"
  role="contentinfo"
  style="--snj-footer-padding-top: {{ section.settings.padding-block-start }}px; --snj-footer-padding-bottom: {{ section.settings.padding-block-end }}px;"
>
  <div class="snj-footer__container">

    {%- comment -%} Background image {%- endcomment -%}
    {%- if section.settings.background_image != blank or section.settings.background_image_mobile != blank -%}
      <picture class="snj-footer__bg">
        {%- if section.settings.background_image != blank -%}
          <source media="(min-width: 750px)" srcset="{{ section.settings.background_image | image_url: width: 2000 }}">
        {%- endif -%}
        {%- if section.settings.background_image_mobile != blank -%}
          <img
            src="{{ section.settings.background_image_mobile | image_url: width: 800 }}"
            alt=""
            class="snj-footer__bg-img"
            loading="lazy"
            width="{{ section.settings.background_image_mobile.width }}"
            height="{{ section.settings.background_image_mobile.height }}"
          >
        {%- elsif section.settings.background_image != blank -%}
          <img
            src="{{ section.settings.background_image | image_url: width: 2000 }}"
            alt=""
            class="snj-footer__bg-img"
            loading="lazy"
            width="{{ section.settings.background_image.width }}"
            height="{{ section.settings.background_image.height }}"
          >
        {%- endif -%}
      </picture>
    {%- endif -%}

    <div class="snj-footer__inner">

      {%- comment -%} ── Nav columns (left side) ── {%- endcomment -%}
      <div class="snj-footer__nav-columns">

        {%- comment -%} Explore products {%- endcomment -%}
        <div class="snj-footer__column">
          {%- if section.settings.heading_explore != blank -%}
            <p class="snj-footer__column-label">{{ section.settings.heading_explore | escape }}</p>
          {%- endif -%}
          <div class="snj-footer__brand-logos">
            {%- comment -%} Brand logo 1 — LIVEENHANCED {%- endcomment -%}
            {%- if section.settings.brand_logo_1 != blank -%}
              {%- if section.settings.product_link_1 != blank -%}<a href="{{ section.settings.product_link_1 }}" class="snj-footer__brand-logo-link">{%- endif -%}
              <img
                src="{{ section.settings.brand_logo_1 | image_url: width: 300 }}"
                alt="{{ section.settings.brand_logo_1.alt | default: 'Brand logo' | escape }}"
                class="snj-footer__brand-logo-img"
                loading="lazy"
                width="{{ section.settings.brand_logo_1.width }}"
                height="{{ section.settings.brand_logo_1.height }}"
              >
              {%- if section.settings.product_link_1 != blank -%}</a>{%- endif -%}
            {%- else -%}
              {%- if section.settings.product_link_1 != blank -%}<a href="{{ section.settings.product_link_1 }}" class="snj-footer__brand-logo-link">{%- endif -%}
              <img
                src="{{ 'liveenhanced-1.svg' | asset_url }}"
                alt="LIVEENHANCED"
                class="snj-footer__brand-logo-img"
                loading="lazy"
                width="117"
                height="12"
              >
              {%- if section.settings.product_link_1 != blank -%}</a>{%- endif -%}
            {%- endif -%}

            {%- comment -%} Brand logo 2 — LONGER {%- endcomment -%}
            {%- if section.settings.brand_logo_2 != blank -%}
              {%- if section.settings.product_link_2 != blank -%}<a href="{{ section.settings.product_link_2 }}" class="snj-footer__brand-logo-link">{%- endif -%}
              <img
                src="{{ section.settings.brand_logo_2 | image_url: width: 300 }}"
                alt="{{ section.settings.brand_logo_2.alt | default: 'Brand logo' | escape }}"
                class="snj-footer__brand-logo-img"
                loading="lazy"
                width="{{ section.settings.brand_logo_2.width }}"
                height="{{ section.settings.brand_logo_2.height }}"
              >
              {%- if section.settings.product_link_2 != blank -%}</a>{%- endif -%}
            {%- endif -%}

            {%- comment -%} Brand logo 3 — STRONGER {%- endcomment -%}
            {%- if section.settings.brand_logo_3 != blank -%}
              {%- if section.settings.product_link_3 != blank -%}<a href="{{ section.settings.product_link_3 }}" class="snj-footer__brand-logo-link">{%- endif -%}
              <img
                src="{{ section.settings.brand_logo_3 | image_url: width: 300 }}"
                alt="{{ section.settings.brand_logo_3.alt | default: 'Brand logo' | escape }}"
                class="snj-footer__brand-logo-img"
                loading="lazy"
                width="{{ section.settings.brand_logo_3.width }}"
                height="{{ section.settings.brand_logo_3.height }}"
              >
              {%- if section.settings.product_link_3 != blank -%}</a>{%- endif -%}
            {%- endif -%}
          </div>
        </div>

        {%- comment -%} Company column {%- endcomment -%}
        <div class="snj-footer__column">
          {%- if section.settings.heading_company != blank -%}
            <p class="snj-footer__column-label">{{ section.settings.heading_company | escape }}</p>
          {%- endif -%}
          {%- if section.settings.link_list_company != blank -%}
            <ul class="snj-footer__link-list">
              {%- for link in linklists[section.settings.link_list_company].links -%}
                <li><a href="{{ link.url }}" class="snj-footer__link">{{ link.title | escape }}</a></li>
              {%- endfor -%}
            </ul>
          {%- endif -%}
        </div>

      </div>

      {%- comment -%} ── Get in touch (right side) ── {%- endcomment -%}
      <div class="snj-footer__contact">
        {%- if section.settings.heading_get_in_touch != blank -%}
          <p class="snj-footer__column-label">{{ section.settings.heading_get_in_touch | escape }}</p>
        {%- endif -%}

        {%- assign has_social = false -%}
        {%- if section.settings.facebook_url != blank or section.settings.pinterest_url != blank or section.settings.instagram_url != blank or section.settings.tiktok_url != blank -%}
          {%- assign has_social = true -%}
        {%- endif -%}

        {%- if has_social -%}
          <div class="snj-footer__social">
            {%- if section.settings.facebook_url != blank -%}
              <a href="{{ section.settings.facebook_url }}" class="snj-footer__social-link" aria-label="Facebook" target="_blank" rel="noopener noreferrer">
                <img src="{{ 'frame.svg' | asset_url }}" alt="" width="20" height="20" loading="lazy">
              </a>
            {%- endif -%}
            {%- if section.settings.pinterest_url != blank -%}
              <a href="{{ section.settings.pinterest_url }}" class="snj-footer__social-link" aria-label="Pinterest" target="_blank" rel="noopener noreferrer">
                <img src="{{ 'frame-2.svg' | asset_url }}" alt="" width="20" height="20" loading="lazy">
              </a>
            {%- endif -%}
            {%- if section.settings.instagram_url != blank -%}
              <a href="{{ section.settings.instagram_url }}" class="snj-footer__social-link" aria-label="Instagram" target="_blank" rel="noopener noreferrer">
                <img src="{{ 'frame-3.svg' | asset_url }}" alt="" width="20" height="20" loading="lazy">
              </a>
            {%- endif -%}
            {%- if section.settings.tiktok_url != blank -%}
              <a href="{{ section.settings.tiktok_url }}" class="snj-footer__social-link" aria-label="TikTok" target="_blank" rel="noopener noreferrer">
                <img src="{{ 'frame-4.svg' | asset_url }}" alt="" width="20" height="20" loading="lazy">
              </a>
            {%- endif -%}
          </div>
        {%- endif -%}

        <div class="snj-footer__media-info">
          {%- if section.settings.media_email != blank -%}
            <p class="snj-footer__media-email">{{ section.settings.media_email | escape }}</p>
          {%- endif -%}
          {%- if section.settings.media_description != blank -%}
            <p class="snj-footer__media-desc">{{ section.settings.media_description | escape }}</p>
          {%- endif -%}
        </div>
      </div>

      {%- comment -%} ── Disclaimer ── {%- endcomment -%}
      {%- if section.settings.show_disclaimer and section.settings.disclaimer_text != blank -%}
        <div class="snj-footer__disclaimer">
          <svg class="snj-footer__disclaimer-icon" width="10" height="10" viewBox="0 0 10 10" fill="none" aria-hidden="true">
            <path d="M5 0V10" stroke="white" stroke-width="1.5"/>
            <path d="M0 5H10" stroke="white" stroke-width="1.5"/>
          </svg>
          <p class="snj-footer__disclaimer-text">{{ section.settings.disclaimer_text | escape }}</p>
        </div>
      {%- endif -%}

      {%- comment -%} ── Legal (policies + copyright) ── {%- endcomment -%}
      <div class="snj-footer__legal">
        {%- if section.settings.link_list_policies != blank -%}
          <div class="snj-footer__policies">
            {%- for link in linklists[section.settings.link_list_policies].links -%}
              <a href="{{ link.url }}" class="snj-footer__policy-link">{{ link.title | escape }}</a>
            {%- endfor -%}
          </div>
        {%- endif -%}
        {%- if section.settings.copyright_text != blank -%}
          <p class="snj-footer__copyright">{{ section.settings.copyright_text | escape }}</p>
        {%- endif -%}
      </div>

      {%- comment -%} ── Large footer logo ── {%- endcomment -%}
      <div class="snj-footer__large-logo">
        {%- if section.settings.footer_logo != blank -%}
          <img
            src="{{ section.settings.footer_logo | image_url: width: 2000 }}"
            alt="{{ shop.name | escape }}"
            class="snj-footer__large-logo-img"
            loading="lazy"
          >
        {%- else -%}
          <img
            src="{{ 'logos.svg' | asset_url }}"
            alt="{{ shop.name | escape }}"
            class="snj-footer__large-logo-img"
            loading="lazy"
            width="150"
            height="16"
          >
        {%- endif -%}
      </div>

    </div>
  </div>
</footer>

{% stylesheet %}
/* ====================================================================
   SNJ Footer — Enhanced Redesign
   Matches desktop Figma: 32px padding, 12px radius, grid layout
   Mobile: stacked, 16px padding, 8px radius
   ==================================================================== */

.snj-footer {
  font-family: var(--font-body--family);
  padding-top: var(--snj-footer-padding-top, 0px);
  padding-bottom: var(--snj-footer-padding-bottom, 0px);
}

/* ── Container ── */
.snj-footer__container {
  position: relative;
  border-radius: 12px;
  overflow: hidden;
  padding: 32px;
  background-color: #0a0a14;
}

/* ── Background image ── */
.snj-footer__bg {
  position: absolute;
  inset: 0;
  z-index: 0;
  pointer-events: none;
}

.snj-footer__bg-img {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/* ── Inner grid layout ── */
.snj-footer__inner {
  position: relative;
  z-index: 1;
  display: grid;
  grid-template-columns: 1fr auto;
  row-gap: 48px;
  column-gap: 40px;
  grid-template-areas:
    "nav     contact"
    "disclaimer disclaimer"
    "legal   legal"
    "logo    logo";
}

/* ── Navigation columns (left side) ── */
.snj-footer__nav-columns {
  grid-area: nav;
  display: flex;
  gap: 80px;
  align-items: flex-start;
}

.snj-footer__column {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.snj-footer__column-label {
  font-family: var(--font-body--family);
  font-size: 13px;
  font-weight: 400;
  line-height: 1.3;
  letter-spacing: -0.3px;
  color: #A6A6A6;
  margin: 0;
}

/* ── Brand logos (Explore products) ── */
.snj-footer__brand-logos {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.snj-footer__brand-logo-link {
  display: block;
  text-decoration: none;
  transition: opacity 0.2s ease;
}

.snj-footer__brand-logo-link:hover {
  opacity: 0.7;
}

/* All brand logos are displayed white-on-dark — invert black/colored assets to white */
.snj-footer__brand-logo-img {
  display: block;
  height: auto;
  max-width: 160px;
  filter: brightness(0) invert(1);
}

/* ── Company nav links ── */
.snj-footer__link-list {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.snj-footer__link-list li {
  margin: 0;
  padding: 0;
}

.snj-footer__link {
  font-family: var(--font-body--family);
  font-size: 14px;
  font-weight: 500;
  line-height: 1.2;
  color: #ffffff;
  text-decoration: none;
  display: block;
  transition: opacity 0.2s ease;
}

.snj-footer__link:hover {
  opacity: 0.7;
}

/* ── Get in touch (right side) ── */
.snj-footer__contact {
  grid-area: contact;
  display: flex;
  flex-direction: column;
  gap: 20px;
  align-items: flex-start;
}

.snj-footer__social {
  display: flex;
  align-items: center;
  gap: 24px;
}

.snj-footer__social-link {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 20px;
  height: 20px;
  text-decoration: none;
  transition: opacity 0.2s ease;
}

.snj-footer__social-link:hover {
  opacity: 0.7;
}

.snj-footer__social-link img {
  display: block;
  width: 20px;
  height: 20px;
}

.snj-footer__media-info {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.snj-footer__media-email {
  font-family: var(--font-body--family);
  font-size: 14px;
  font-weight: 500;
  line-height: 1.3;
  color: #ffffff;
  margin: 0;
}

.snj-footer__media-desc {
  font-family: var(--font-body--family);
  font-size: 12px;
  font-weight: 400;
  line-height: 1.4;
  color: #A6A6A6;
  margin: 0;
}

/* ── Disclaimer ── */
.snj-footer__disclaimer {
  grid-area: disclaimer;
  display: flex;
  align-items: flex-start;
  gap: 8px;
}

.snj-footer__disclaimer-icon {
  flex-shrink: 0;
  width: 10px;
  height: 10px;
  margin-top: 3px;
}

.snj-footer__disclaimer-text {
  font-family: var(--font-body--family);
  font-size: 12px;
  font-weight: 400;
  line-height: 1.5;
  color: #ffffff;
  margin: 0;
}

/* ── Legal (policies + copyright) ── */
.snj-footer__legal {
  grid-area: legal;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.snj-footer__policies {
  display: flex;
  flex-wrap: wrap;
  gap: 4px 16px;
}

.snj-footer__policy-link {
  font-family: var(--font-body--family);
  font-size: 12px;
  font-weight: 400;
  line-height: 1.4;
  color: #A6A6A6;
  text-decoration: none;
  transition: color 0.2s ease;
}

.snj-footer__policy-link:hover {
  color: #ffffff;
}

.snj-footer__copyright {
  font-family: var(--font-body--family);
  font-size: 12px;
  font-weight: 400;
  line-height: 1.4;
  color: #A6A6A6;
  margin: 0;
}

/* ── Large footer logo ── */
.snj-footer__large-logo {
  grid-area: logo;
  overflow: hidden;
}

/* Always invert to white — footer is always on a dark background */
.snj-footer__large-logo-img {
  display: block;
  width: 100%;
  height: auto;
  filter: brightness(0) invert(1);
}

/* ── Mobile ── */
@media screen and (max-width: 749px) {
  .snj-footer__container {
    border-radius: 8px;
    padding: 20px 16px 24px;
  }

  .snj-footer__inner {
    display: flex;
    flex-direction: column;
    gap: 32px;
  }

  .snj-footer__nav-columns {
    gap: 32px;
    flex-wrap: wrap;
  }

  .snj-footer__contact {
    gap: 16px;
  }

  .snj-footer__social {
    gap: 20px;
  }

  .snj-footer__media-email {
    font-size: 13px;
  }

  .snj-footer__legal {
    gap: 6px;
  }

  .snj-footer__policies {
    gap: 4px 12px;
  }

  .snj-footer__policy-link,
  .snj-footer__copyright {
    color: #A6A6A6;
  }
}
{% endstylesheet %}

{% schema %}
{
  "name": "t:names.custom_footer",
  "tag": "div",
  "enabled_on": {
    "groups": ["footer"]
  },
  "settings": [
    {
      "type": "header",
      "content": "t:content.background_image"
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "t:settings.image"
    },
    {
      "type": "image_picker",
      "id": "background_image_mobile",
      "label": "t:settings.background_image_mobile"
    },
    {
      "type": "header",
      "content": "t:content.navigation"
    },
    {
      "type": "text",
      "id": "heading_explore",
      "label": "t:settings.heading_explore",
      "default": "Explore products"
    },
    {
      "type": "image_picker",
      "id": "brand_logo_1",
      "label": "t:settings.product_logo_1"
    },
    {
      "type": "url",
      "id": "product_link_1",
      "label": "t:settings.product_link_1"
    },
    {
      "type": "image_picker",
      "id": "brand_logo_2",
      "label": "t:settings.product_logo_2"
    },
    {
      "type": "url",
      "id": "product_link_2",
      "label": "t:settings.product_link_2"
    },
    {
      "type": "image_picker",
      "id": "brand_logo_3",
      "label": "t:settings.product_logo_3"
    },
    {
      "type": "url",
      "id": "product_link_3",
      "label": "t:settings.product_link_3"
    },
    {
      "type": "text",
      "id": "heading_company",
      "label": "t:settings.heading_company",
      "default": "Company"
    },
    {
      "type": "link_list",
      "id": "link_list_company",
      "label": "t:settings.company_menu"
    },
    {
      "type": "header",
      "content": "t:content.contact"
    },
    {
      "type": "text",
      "id": "heading_get_in_touch",
      "label": "t:settings.heading_contact",
      "default": "Get in touch"
    },
    {
      "type": "url",
      "id": "facebook_url",
      "label": "t:settings.facebook_url"
    },
    {
      "type": "url",
      "id": "pinterest_url",
      "label": "t:settings.pinterest_url"
    },
    {
      "type": "url",
      "id": "instagram_url",
      "label": "t:settings.instagram_url"
    },
    {
      "type": "url",
      "id": "tiktok_url",
      "label": "t:settings.tiktok_url"
    },
    {
      "type": "text",
      "id": "media_email",
      "label": "t:settings.contact_email",
      "default": "media@enhanced.com"
    },
    {
      "type": "text",
      "id": "media_description",
      "label": "t:settings.contact_description",
      "default": "Important: For media enquiries or interview requests only."
    },
    {
      "type": "header",
      "content": "t:content.disclaimer"
    },
    {
      "type": "checkbox",
      "id": "show_disclaimer",
      "label": "t:settings.show_disclaimer",
      "default": true
    },
    {
      "type": "textarea",
      "id": "disclaimer_text",
      "label": "t:settings.disclaimer_text",
      "default": "The Product is not a medicinal product and does not diagnose, treat, cure, or prevent disease."
    },
    {
      "type": "header",
      "content": "t:content.legal"
    },
    {
      "type": "link_list",
      "id": "link_list_policies",
      "label": "t:settings.link_list_policies"
    },
    {
      "type": "textarea",
      "id": "copyright_text",
      "label": "t:settings.copyright_text"
    },
    {
      "type": "header",
      "content": "t:content.branding"
    },
    {
      "type": "image_picker",
      "id": "footer_logo",
      "label": "t:settings.footer_logo"
    },
    {
      "type": "header",
      "content": "t:content.colors"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:settings.color_scheme",
      "default": "scheme-5"
    },
    {
      "type": "header",
      "content": "t:content.padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.top",
      "min": 0,
      "max": 100,
      "step": 2,
      "default": 0,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.bottom",
      "min": 0,
      "max": 100,
      "step": 2,
      "default": 0,
      "unit": "px"
    }
  ],
  "presets": [
    {
      "name": "t:names.custom_footer"
    }
  ]
}
{% endschema %}
